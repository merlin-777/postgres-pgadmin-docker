
https://hub.docker.com/r/elestio/pgadmin

init pgadmin
```bash
docker-compose up -d
```

check status
```bash
docker-compose ps
```

stop process
```bash
docker-compose stop
```

## Connect in pgadming
use name's services of docker-compose
"postgresDb" and port "5432"